import { ForObjectPipe } from './for-object.pipe';

describe('ForObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new ForObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
